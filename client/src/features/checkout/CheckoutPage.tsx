import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant="h3" >Only authenticated users can access this page.</Typography>
    )
}
